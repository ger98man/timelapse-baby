// Удостоверение приложения. Заполняется автором один раз перед выкладкой и
// одинаково для всех, кто им пользуется: Client ID — это идентификатор
// программы, а не человека. Секретом он не является и в браузерных приложениях
// всегда открыт — доступ к данным даёт не он, а вход пользователя в свой Google.
//
// Всё пользовательское (имя, дата рождения, настройки кадра) здесь не хранится:
// оно лежит в config.json в папке на Диске владельца и приезжает после входа.

export const GOOGLE = {
  // OAuth Client ID из Google Cloud Console, тип «Web application».
  // В Authorized JavaScript origins должны быть перечислены адреса, откуда
  // приложение открывают, — боевой и http://localhost:8787 для разработки.
  clientId: '',

  // API key — нужен для окна выбора общей папки (второй родитель).
  apiKey: '',

  // Имя папки, которую приложение создаёт в Диске при первом входе.
  folderName: 'Каждый день',

  // Кого пускать. Пустой список — пускать всех, кто знает ссылку.
  // Это удобство, а не защита: настоящий замок — права доступа к папке
  // в самом Google Диске, их подделать нельзя.
  allowedEmails: [],
};

export const configured = () => Boolean(GOOGLE.clientId);
export const pickerReady = () => Boolean(GOOGLE.apiKey);
